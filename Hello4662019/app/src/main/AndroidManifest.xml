<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ifpi.hello4662019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".NotasSituacaoIntentActivity"
            android:parentActivityName=".NotasIntentActivity">

        </activity>

        <activity android:name=".NotasIntentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LinearLayoutActivity"></activity>
        <activity android:name=".IMCActivity" />
        <activity android:name=".ContadorActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Tela2Activity" />
    </application>

</manifest>