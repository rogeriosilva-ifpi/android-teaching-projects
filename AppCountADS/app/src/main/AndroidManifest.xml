<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rogermac.appcountads">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity" />
        <activity android:name=".LayoutLLActivity" />
        <activity
            android:name=".SignupActivity"
            android:parentActivityName=".LayoutLLActivity" />
        <activity android:name=".Intent4ResultActivity"></activity>
        <activity android:name=".Intent4ResultTela2Activity" />
        <activity android:name=".MenuActivity">

        </activity>
        <activity
            android:name=".ListaJogosActivity"
            android:label="@string/title_activity_lista_jogos"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>